name: Ziffer
options:
  bundleIdPrefix: com.pietz
  createIntermediateGroups: true
  deploymentTarget:
    macOS: "15.0"
  developmentLanguage: de
  groupSortPosition: top
  minimumXcodeGenVersion: "2.42"

fileGroups:
  - Package.swift
  - README.md
  - docs

packages:
  ZifferPackages:
    path: .

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: 34MWWCL4H2
    CODE_SIGN_STYLE: Manual
    CODE_SIGN_IDENTITY: Apple Development
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    GCC_WARN_ABOUT_RETURN_TYPE: YES
    CLANG_WARN_DOCUMENTATION_COMMENTS: YES

targets:
  Ziffer:
    type: application
    platform: macOS
    sources:
      - path: App
    dependencies:
      - package: ZifferPackages
        product: Domain
      - package: ZifferPackages
        product: Database
      - package: ZifferPackages
        product: DocumentStore
      - package: ZifferPackages
        product: AI
      - package: ZifferPackages
        product: ImportPipeline
      - package: ZifferPackages
        product: Analysis
    settings:
      base:
        PRODUCT_NAME: Ziffer
        PRODUCT_BUNDLE_IDENTIFIER: com.pietz.ziffer
        INFOPLIST_FILE: App/Info.plist
        CODE_SIGN_ENTITLEMENTS: App/Ziffer.entitlements
        ENABLE_HARDENED_RUNTIME: YES
        COMBINE_HIDPI_IMAGES: YES
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        SWIFT_EMIT_LOC_STRINGS: YES
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks"

schemes:
  Ziffer:
    build:
      targets:
        Ziffer: all
    run:
      config: Debug
    archive:
      config: Release
