# Status

**Release 0.9.0 veröffentlicht (Build 11, 2026-09-22).** Tag `v0.9.0` auf `da49d4d`, Hauptzweig und Tag gepusht, [GitHub-Release](https://github.com/pietz/pfennig/releases/tag/v0.9.0) mit sechs Assets als Latest veröffentlicht. Universal-Build für Apple Silicon und Intel, Developer-ID-Signatur, Apple-Notarisierung `Accepted`, Stapling, Gatekeeper und Ed25519-Updateprüfung mit eingebettetem öffentlichem Schlüssel erfolgreich. Alle öffentlichen Downloads und der Latest-Appcast stimmen byteweise mit den geprüften lokalen Artefakten überein. 220 Tests erfolgreich. Das aus dem Distributions-ZIP entpackte Release startet mit neuem, leerem Releasearchiv; kein separater macOS-Testaccount und keine Installation nach `/Applications`.

Enthält die feldbezogene Pflichtprüfung und nullable Steuerbehandlung. Das manuelle [0.8.0-Arbeitsblatt](maintenance/0.9.0.md) bleibt außerhalb der App. An synthetischen gefüllten und leeren 0.8.0-Datenbanken geprüft: sämtliche Buchungsfelder bis auf die beabsichtigte `unklar`-zu-`NULL`-Normalisierung erhalten, andere Tabellen unverändert, Aktivitäts-Snapshots normalisiert, höchster ID-Zähler erhalten, neue leere Steuerwerte speicherbar, erneute Ausführung abgewiesen und bei spätem Fehler vollständig zurückgerollt. Release-Vertragsprüfung erfolgreich. Nachtrag 2026-09-23: Der Eigentümer hat das Archiv auf dem genutzten Rechner umgestellt und 0.9.0 installiert; Buchungen mussten nicht gelöscht werden. Releasehinweise verlinken die nötigen Schritte vor dem ersten Öffnen des alten Archivs mit 0.9.0.

**Entwürfe und Bestätigung (2026-09-22).** Steuerbehandlung ist im einzelnen Schema nullable; der Enumwert `unklar` und seine Promptanweisung entfallen. Der Inspector zeigt „Bitte auswählen“. Bestehende Core-Prüfregeln liefern feldbezogene Befunde für Bestätigung, Startaufgaben, Listenstatus und Filter. Fehlende Angaben bleiben als Entwurf speicherbar, auch durch das SQL-Werkzeug; ungültige Agentenwerte werden weiter zurückgewiesen. Manuelle Buchungen starten unbestätigt ohne vorausgewählte Steuerbehandlung. Titel, Kategorie, Steuerbehandlung und Positionen sowie bedingte Länder-/Fremdwährungsangaben müssen vor Bestätigung vollständig sein. Unvollständige alte Bestätigungen bleiben sichtbar; eine Korrektur oder eine ungültig machende Nutzeränderung braucht erneute Bestätigung. Agentenherkunft stammt aus dem vorhandenen Aktivitätslog: fehlende/ungültige Felder dort rot, manuelle Entwürfe neutral. Keine zusätzlichen Warnregeln; Berechnung und Exportpolitik erhalten, Exporthinweise verwenden die neue fehlende Auswahl.

Verifikation: separate Prüfung auf Korrektheit und unnötige Komplexität, SwiftFormat, 220 Tests und Debug-App-Build erfolgreich. Native UI mit synthetischen Dev-Daten geprüft: neutrale manuelle Auswahl, rote Agentenfelder, bedingter Steuersatzhinweis, gesperrte/freigegebene Bestätigung und Entfernen aus „Zu prüfen“ nach Bestätigung. Leeren der Steuerauswahl erhält Beträge. Testdatensätze anschließend entfernt. Vorher existierte hier kein Dev- oder Releasearchiv. Keine Installation, Veröffentlichung oder Push. Der Eigentümer verwendet zwei Rechner; bestehende Datenbanken sollen separat gesichert und manuell korrigiert werden, ohne Migrationen oder Rückwärtskompatibilität im Code. Die bestehenden Archive waren in dieser Umgebung nicht verfügbar und sind noch nicht umgestellt.

**Aktivitätsanzeige verworfen (2026-09-22).** Der Eigentümer nimmt #18 aus dem aktuellen Produktumfang: keine Kernfunktion und derzeit keine UI dafür. Die vorhandenen Aktivitätsdaten und ihre Protokollierung bleiben unverändert für einen möglichen späteren Bedarf. Backlog und Scope-Gedächtnis angepasst; historische Einträge darunter beschreiben den früheren Plan.

**Importsicherheit nach Review (2026-09-22).** Während laufender Importe ist das Löschen von Buchungen gesperrt: Tastatur und Kontextmenü sowie eine bereits offene Löschbestätigung beachten den bestehenden Laufstatus, zusätzlich schützt der zentrale Löschweg im AppModel. Dadurch kann eine frühe Buchung nicht ihren Beleg entfernen, während derselbe Agentenlauf weitere Vorgänge daraus erfasst. Rekursive Ordnersuche läuft außerhalb des Main Actors; Sortierung je Drop, Grenze von 50 Dateien und genaue Gesamtzahl bleiben erhalten. Keine zusätzliche Zustandsverwaltung oder Belegbesitzer-Infrastruktur. Astra setzte die beiden Änderungen um, der Hauptagent prüfte unabhängig Korrektheit und unnötige Komplexität. Bestehender Ordnersuchtest, vollständige Suite mit 213 Tests, SwiftFormat und Debug-Build erfolgreich. Keine neue App-Testinfrastruktur und kein nativer UI-Laufzeittest. Keine Archivänderung.

**Parallele Entwicklungsstände zusammengeführt (2026-09-22).** Die 14 zusätzlichen Remote-Commits sind mit den 39 lokalen Commits inhaltlich abgeglichen. Mehrfachauswahl und Finder-Öffnen bleiben erhalten; beide verwenden die bestehenden Buchungs-/Eingangswege. Sammellöschung entfernt nur zuletzt verwaiste Belege und protokolliert jede Buchung in derselben Transaktion. Bei Speicherfehler stellt die App auch Liste und Auswahl wieder her. Importabschluss bleibt dauerhaft und unabhängig von der Diagnose; `noBooking`, Beleg-only-Eingang, knappe Notizen und mehrere belegte Vorgänge pro Datei bleiben erhalten. Der optionale Live-Test erwartet deshalb einen abgewiesenen Kontoauszug ohne Buchungsänderung, nicht automatischen Zahlungsabgleich.

Die früher beschlossene proportionale Inlandsvorsteuer bleibt erhalten, einschließlich der bekannten Grenze für inländische Gegenstände unter zehn Prozent betrieblicher Nutzung. Die explizite EU-Warenbehandlung behält dagegen ihre gesetzliche Zehn-Prozent-Grenze. Leseregeln zu Rundung/Zukunftsdatum gelten nur für den Agenten; sachliche Regeln weiterhin auch beim Bestätigen. Dezimale Eingangssteuersätze sind unterstützt, Einnahmen/Empfängersteuer weiter auf die vorhandenen Sätze begrenzt. Quellen und Abgrenzung stehen in `docs/research-ustva-xml.md`.

SwiftFormat, Testsuite mit 213 Tests (ein Live-Test übersprungen), Debug- und Release-Konfigurationsbuild, Property-List- und Release-Vertragsprüfung erfolgreich. Gebaute Kennungen, Icons und alternative Dateizuordnungen beider Apps geprüft; unveränderte Paket-Pins bestätigt und generierte Hashänderung entfernt. Unabhängige Prüfung und Hauptagenten-Integration abgeschlossen, einschließlich Fehlerpfadkorrektur und Regressionen für gemeinsame Belege, atomaren Rollback, Importabschluss und kombinierte Steuerregeln. Kein Laufzeittest der nativen UI oder Finder-Öffnung, kein Live-API-/ELSTER-Test. Die Remote-Versionsangabe 0.7.2 (Build 9) ist übernommen, kein Release veröffentlicht oder installiert. Keine zusätzliche Schemaänderung, keine Archivumstellung und keine Änderung der installierten App oder ihrer Daten. Chat bleibt ohne Umsetzungsauftrag.

**Zeiträume anderweitig erledigt markieren (2026-09-22).** Im vorhandenen Exportfenster markiert eine native Aktion den gewählten UStVA- oder EÜR-Zeitraum als anderweitig erledigt. Sie nutzt denselben bestehenden Zeitstempel wie ein Dateiexport und entfernt die Erinnerung; keine Datei, Übermittlung, Schemaänderung oder neue Herkunftsverwaltung. Exporthinweis und Inspector sprechen deshalb ehrlich von Export oder Erledigt-Markierung. Das Fenster bleibt offen, Speicherfehler erreichen den bestehenden Fehlerdialog. Datenübernahme bleibt Nutzeraufgabe, ohne Startdatum oder Migrationssystem; unterjähriger Wechsel braucht für die Jahres-EÜR beide Datenbestände. 202 Tests, SwiftFormat und App-Build erfolgreich. Regressionen prüfen beide Zeitraumarten, persistierte Markierung, gezieltes Ausblenden der Erinnerung und unveränderte Buchungen/Steuerwerte. Unabhängige Prüfung durch den Hauptagenten abgeschlossen; kein manueller UI-Test. #2 im abgestimmten Umfang abgeschlossen, Chat #16 bleibt bis zur konkreten Planfreigabe ohne Implementierungsauftrag.

**Knappe Notizen und konkrete Unsicherheiten (2026-09-22).** Eine generische Agentenanweisung reserviert `notizen` für relevante Zusatzinformationen oder Unsicherheiten mit Grund/Prüfhinweis; sonst bleiben neue Notizen leer. Keine Zusammenfassungen gespeicherter Felder, übliche Zahlungs-/Nutzungsannahmen nicht als Unsicherheit behandeln, inhaltliche Nutzernotizen erhalten. Tatsächlich unbekannte Steuerzuordnung nutzt `unklar`. Promptaudit fand keine bisherige Zusammenfassungsanweisung; Umrechnung liefert Kursprovenienz als `notiz`, unverändert. Keine Schema-, UI- oder Berechnungsänderung. Promptvertrag ergänzt, SwiftFormat, 201 Tests und App-Build erfolgreich; unabhängige Prüfung durch den Hauptagenten ohne Befund abgeschlossen. Kein Live-Modelltest. #5 ist im freigegebenen Umfang abgeschlossen; ein späterer Fragekanal gehört zum Chat.

**Belegte Geschäftsvorgänge statt einer Buchung je Dokument (2026-09-22).** Eigentümerkorrektur zu #6: vorhandene Primitive nutzen, fehlende Spezialfunktionen nicht mit fehlender Unterstützung gleichsetzen. Agentenanweisung erlaubt mehrere eigenständige Vorgänge mit derselben Belegdatei und nennt belegte Verrechnungen als Zahlungen; bestehende Vorgänge weiterhin ergänzen statt doppelt buchen. Kategorie und Privatanteil bleiben buchungsweit. 100 Euro vereinnahmter Umsatz und 3 Euro einbehaltene Gebühr sind zwei bezahlte Buchungen, 97 Euro Banktransfer keine zusätzliche Einnahme. Keine Schema-/Steuerberechnungsänderung, Plattformintegration oder automatische Abstimmung; bloße Auszahlungsbestätigungen erlauben keine universelle Rekonstruktion und Merchants of Record kein pauschales Hochrechnen. Die vorherige pauschale Zurückstellung ist korrigiert. Promptvertrag und synthetischer Importtest mit zwei bezahlten Buchungen und gemeinsamem Beleg sichern die Änderung; keine Prüfung echter Modellinterpretation oder neuer Steuerregeln. SwiftFormat, 200 Tests und App-Build erfolgreich; unabhängiges Astra-Medium-Review ohne Befund, Integrationsprüfung abgeschlossen. #6 ist mit dieser generischen Lösung abgeschlossen, nicht mit einer Plattformintegration. Nächstes Diskussionsthema bleibt #2, Übernahme bestehender Buchhaltung.

**Exporthinweis bei unklarer Steuerbehandlung (2026-09-22).** Eigentümerentscheidung zu #20: ungeprüfte Buchungen bleiben in EÜR und UStVA enthalten; kein pauschaler Ausschluss von `nur_zahlung` und keine Exportsperre. Die bestehende Berechnung bleibt unverändert: EÜR nutzt die vorhandenen Angaben, UStVA lässt `steuerbehandlung = unklar` mangels Kennzahlzuordnung aus. Das Exportfenster zeigt jetzt die Zahl solcher Buchungen im gewählten Zeitraum und erläutert die jeweilige Folge, unabhängig vom Prüfstatus. Zählung über das vorhandene `Zeitraum.beruehrt`, einschließlich Zahlungsdaten und laufender EÜR-AfA; ignorierte Buchungen zählen nicht. Keine neuen Felder, Agentenregeln oder Prüfschritte. 199 Tests, SwiftFormat und App-Build erfolgreich; unabhängiges Astra-Medium-Review ohne Befund zu Korrektheit oder unnötiger Komplexität, abschließende Integrationsprüfung abgeschlossen. Regressionen sichern beide Richtungen, `nur_zahlung`, Bestätigung ohne Berechnungsänderung, unklare bestätigte Buchungen, Zeitraumgrenzen, Ignorieren und AfA. Kein manueller UI-Test. Nächstes Bestandsthema ist #6, Plattformauszahlungen.

**Innergemeinschaftlicher Erwerb (2026-09-22).** Ein zusätzlicher Wert `innergemeinschaftlicher_erwerb` in `steuerbehandlung`, keine neue Spalte oder Tabelle. Der Agent liest die knappe Abgrenzung zu grenzüberschreitenden Dienstleistungen im Schema; die Kategorien bleiben unabhängig. Kz 89/93 nehmen die Erwerbsbemessungen zu 19/7 Prozent auf, Kz 61 die abziehbare Erwerbsteuer unter Berücksichtigung des Privatanteils und der Zehn-Prozent-Grenze. Kleinunternehmer erhalten keinen Vorsteuerabzug, aber die passende UStVA-Frist bei steuerpflichtigem Erwerb. Positionen behalten den anwendbaren Satz und `steuer = 0`; Zahlungen, Lieferantenbetrag und EÜR bekommen keine fiktive Steuer. Inspector und Validierung teilen die vorhandene Empfängersteuer-Logik, der doppelte Inspector-Zweig entfällt. Unterstützt ist der gewöhnliche abgeschlossene Erwerb zum Belegdatum; abweichende Zeitpunkte, gemischte Steuerbehandlungen, Schwellen-/Verzichtsprüfung, eigene EU-Warenlieferungen und Drittlandsimporte bleiben außerhalb dieses Schritts. Quellen und Grenzen stehen in `docs/research-ustva-xml.md`.

195 Tests, SwiftFormat und App-Build erfolgreich; eigene separate Schlussprüfung auf Korrektheit und unnötige Komplexität abgeschlossen. Kein Live-Agentenimport, manueller UI-Test oder ELSTER-Testupload für die neuen Kennzahlen. Das bereits geschlossene Entwicklungsarchiv wurde aus der einzigen Schemadefinition neu aufgebaut; sämtliche Zeilen, Sequenzen und Archivdateien bleiben erhalten und wurden geprüft. Backup: `~/Library/Application Support/Pfennig-Dev/Backups/before-eu-acquisitions-20260922-113549/`. Release-App und Release-Archiv unverändert; keine Migration oder Kompatibilitätsschicht. #12 wurde am 2026-09-23 geschlossen; eigene EU-Warenlieferungen (Kz 41) sind in `docs/backlog.md` geparkt. Die Exportpolitik für ungeklärte Buchungen (#20) wurde dabei nicht geändert.

**AGENTS auf dauerhafte Prinzipien reduziert (2026-09-22).** Produktüberblick, Modulgrenzen, schlanke Agentenanweisungen, Datenkonventionen, Arbeitsweise sowie Sicherheits- und Releasegrenzen bleiben in `AGENTS.md`. Flüchtige Angaben wie Tabellenlisten, aktuelle Sidebar-Seiten, offene Issue-Nummern und geplante Features sind entfernt; Memory, Status und Backlog haben klar getrennte Aufgaben. Die Reviewregel entspricht der Eigentümervorgabe zur direkten Zusammenarbeit ohne neue Subagenten. Release-Details bleiben in `docs/releasing.md`. Den ausdrücklich zur Löschung freigegebenen Symlink `CLAUDE.md` entfernt, `AGENTS.md` selbst erhalten. Dokumentationsprüfung und SwiftFormat sauber.

**Sondervorauszahlung für monatliche Dauerfristverlängerung (2026-09-22).** Im Profil erscheint bei Regelbesteuerung, monatlichem Rhythmus und Dauerfristverlängerung die manuelle Eingabe eines festgesetzten Betrags je Jahr. Speicherung als EUR-Cent-Werte im bestehenden Einstellungsschlüssel `sondervorauszahlungen`, keine Schemaänderung oder Archivumstellung. Im Dezember des passenden Jahres steht der positive Betrag in Kz 39 und mindert Kz 83, auch bis zu einem Überschuss; andere Zeiträume und Profile bleiben unverändert. Keine automatische Berechnung, Anmeldung oder Zahlungsbuchung. Die EÜR berücksichtigt weiterhin nur tatsächlich gebuchte Zahlungen, ohne zusätzlichen Aufwand durch die Profileingabe. Abweichende letzte Meldezeiträume bleiben zur manuellen Korrektur in ELSTER; der Profilhinweis sagt dies. Amtliche Quellen und die Grenze der XML-Verifikation stehen im Nachtrag von `docs/research-ustva-xml.md`. 186 Tests einschließlich Jahreszuordnung, Überschuss, XML-Centformat, Profil-Rundlauf und EÜR-Abgrenzung sowie App-Build und SwiftFormat erfolgreich; eigene Schlussprüfung auf Umfang und Komplexität abgeschlossen. Ein veraltetes inkrementelles Swift-Testartefakt wurde durch `swift package clean` behoben, nicht durch Kompatibilitätscode. Kein Live-ELSTER-Upload oder manueller UI-Test durchgeführt.

**Scope-Gedächtnis und Einkommensteuergrenze (2026-09-22).** Auf Eigentümerwunsch hält `.memory/MEMORY.md` Scope-Entscheidungen und kurze Begründungen fest, ohne Backlog oder Gesamtspezifikation zu ersetzen. AGENTS verweist beim Sessionstart darauf. Einkommensteuerzahlungen bleiben außerhalb der Buchhaltung in Pfennig; Issue #3 ist als nicht geplant geschlossen. Keine neue Kategorie oder technische Sperre, bestehende Fehlbuchungen werden manuell korrigiert. Nächstes Bestandsthema ist #8, Sondervorauszahlung. Nur Dokumentation geändert, keine App- oder Archivänderung.

**Handoff-Promptkorrekturen abgeschlossen (2026-09-22).** Eigene Ausgangsrechnungen bleiben ohne belegte Zahlung unbezahlt; der Satz ergänzt den vorhandenen Ausgaben-Default. Tatsächlich geflossene Zahlungssumme und relative Zahlungsvorzeichen stehen gemeinsam in einem kurzen Regelpunkt. `SQLTool.allowed` nennt zusätzlich SELECT auf `afa_tabelle`; Berechtigungen unverändert. Bestehende Prompt-, Toolbeschreibungs- und Ablehnungstests prüfen den Text, keine neue Swift-Logik oder Abstraktion. `noBooking` bleibt, eine negative Gutschriften-Sonderregel bleibt verworfen. Die `nur_zahlung`-Anweisung ist zurückgestellt, weil Kontoauszüge nicht mehr automatisch importiert werden; die separate Exportfrage ist in #20 festgehalten. 181 Tests, SwiftFormat und App-Build erfolgreich; abschließende eigene Diff-Prüfung auf Umfang und Komplexität ohne Befund, entsprechend der Eigentümervorgabe ohne weitere Subagenten. Kein Live-API-Test.

**Bestandsthemen vor neuen Funktionen (2026-09-22).** Chat und Dateienübersicht sind auf Eigentümerentscheidung zurückgestellt. Die bisher besprochene Chat-Richtung ist in #16 gesichert; keine Chat-Implementierung, Schemaänderung oder Archivumstellung erfolgt. Zuerst werden die fünf Handoff-Vorschläge abgeschlossen oder entschieden, danach die bestehenden Korrektheitsfragen und übrigen offenen Themen einzeln behandelt. Die aktuelle Reihenfolge steht in `docs/backlog.md`.

**Neuaufbau abgeschlossen, Arbeitsstand bereinigt (2026-09-22).** Die alte Neuaufbau-Spezifikation ist entfernt, ohne Ersatzspezifikation. Code und Tests, AGENTS und der freigegebene Umfang führen die Entwicklung; `docs/backlog.md` trennt aktuellen Schritt, nächste Themen und geparkte Ideen. Zwei unerfüllte Anforderungen bleiben als offene Enhancement-Issues erhalten: Aktivitätsverlauf anzeigen (#18, ohne Undo/Agentenzugriff) und Anfragekosten aus Tokens und Code-Preistabelle berechnen (#19, ohne Dashboard); keine Implementierungszusage. Der automatische Eingang ist auf Rechnungen, Quittungen und Gutschriften begrenzt; andere Dokumente ohne weiteren Auftrag soll der Agent ohne Buchungsänderung ignorieren. Ohne Buchungsänderung bleibt der bestehende `noBooking`-Fehler. Kontoauszüge und andere Kontextunterlagen gehören künftig zum Chat (#16); danach folgt die Dateienübersicht (#17), jeweils mit noch abzustimmendem Umfang. Angenommene Prompt-Präzisierungen und Warnungen/Duplikaterkennung bleiben separat geparkt; keine negative Gutschriften-Sonderregel, `nur_zahlung`-Politik vertagt. Historische Einträge darunter beschreiben ihren damaligen Stand, insbesondere den früher vorgesehenen Kontoauszugimport. Landingpage-Text, Medienbrief und Demo-Importanleitung sind angepasst. Keine neuen Tools, Status oder Swift-Prüfungen. 181 Tests einschließlich des bestehenden No-Booking-, Wiederholungs- und Verwerfen-Verhaltens sowie `scripts/build.sh` erfolgreich; SwiftFormat und Diff-Prüfung sauber. Unabhängiges Astra-Review der Implementierung ohne Befund, Dokumentationsbereinigung separat vom Hauptagenten geprüft. Kein Live-API-Import durchgeführt.

**Fiktionale Importbelege Q3 2026 (2026-09-21).** Unter `sample-data/2026-q3/` liegt der freigegebene Satz mit 28 deutschsprachigen PDFs für Mara Winter, Studio Linden in Hamburg: neun Ausgangsbelege einschließlich Teil-Storno, elf Einkäufe und sechs zusammenhängende Reisebelege für einen Münchner Workshop sowie zwei ausdrücklich auf Zahlungseingänge gefilterte Bank-Umsatzübersichten. Die Rechnungseingänge und Ausgangsrechnungen werden zuerst importiert, die Bankübersichten danach: sieben Eingänge sollen vorhandenen Ausgangsrechnungen zugeordnet werden, nicht neue Buchungen erzeugen. Erwartet sind 26 Buchungen, davon zwei offen. Alle Inhalte sind erfunden, Kennungen ungültige Demo-Platzhalter. Luna-Extra-High-Agenten haben die getrennten Dokumentgruppen erstellt und unabhängig geprüft. Wiederholbare Python-Skripte mit eigenen Inline-Abhängigkeiten, Soll-Manifeste, Importanleitung und Steuerquellen liegen beim Datensatz, ohne App-Abhängigkeiten oder App-Änderungen. Kein API-Import, kein automatisches Seeding, kein Datenbank-Reset und kein Zugriff auf private Belege. Alle fünf Erzeugerskripte erneut erfolgreich ausgeführt; der Datensatz-Prüfer bestätigt PDF-Text, Summen, Daten und Zahlungszuordnung. Die Layouts wurden gerendert und visuell geprüft, die Reise-Steuersätze gegen amtliche Quellen. Alle sieben Bankeingänge stimmen mit den Ausgangsrechnungen überein; der 21. September ist im zweiten Auszug eingeschlossen. SwiftFormat und Diff-Prüfung sauber. Die tatsächliche Importqualität ist noch nicht getestet.

**Distinctive Debug app icon (2026-09-21).** The icon generator now accepts `--dev` and writes `AppIconDev.appiconset` from the transparent coin artwork with the same native macOS geometry and a purple tile. `project.yml` selects it only for Debug; Release continues to use the unchanged white `AppIcon` assets. Debug build and SwiftFormat passed; visual inspection, release-asset checksums, built icon selection, and independent parent review passed. The development app was restarted with the new icon.

**Getrennte Entwicklungsumgebung (2026-09-21).** Debug baut „Pfennig Dev“ mit `com.pietz.pfennig.dev` und speichert Datenbank, Archiv und Inbox unter `~/Library/Application Support/Pfennig-Dev/`; Release behält Name, Kennung und Datenordner. `ArchivePaths.standard` entscheidet per `DEBUG`, ohne Umschalter oder Schemaänderung. Sparkle wird im Debug-Build weder gestartet noch als Update-Menü angeboten. Das Build-Skript erzeugt das Xcode-Projekt bei jedem Build aus `project.yml`, Build/Run verwenden den konfigurationsabhängigen App-Namen. Profil und App-Präferenzen sind getrennt; der bestehende OpenAI-Keychain-Eintrag bleibt bewusst gemeinsam. Keine vorhandenen Daten kopiert, zurückgesetzt oder geändert. Debug und Release gebaut, jeweils 179 Tests sowie Release-Vertragsprüfung erfolgreich; Debug-App gestartet und ihr geöffnetes, leeres SQLite-Archiv per Prozess-Dateihandle geprüft. Unabhängiges Review ohne Blocker abgeschlossen. Sparkle bleibt im Debug-Bundle eingebettet, wird dort aber nicht ausgeführt; eine separate Abhängigkeitskonfiguration ist für diese Trennung nicht nötig. Nächster separater Schritt: fiktionale, visuell abwechslungsreiche Belege für eine noch abzustimmende Beispielselbstständigkeit und ein bewusst ausgelöstes Befüllen/Zurücksetzen des Entwicklungsarchivs; kein automatisches Zurücksetzen beim Start.

**Ordner ablegen (2026-09-21).** Ein abgelegter Ordner wurde bisher stillschweigend verworfen, weil der Drop-Handler nur nach Dateiendung filterte. `FileIntake.files(in:)` löst jetzt jede abgelegte URL auf: zugelassene Dateien bleiben, Ordner werden rekursiv bis zur letzten zugelassenen Datei durchsucht, versteckte Dateien und Ordner ausgenommen, Pakete wie Numbers-Dokumente gelten als Dateien und werden nicht geöffnet, eine über Ordner und Unterordner doppelt erreichte Datei geht einmal; mehrere Ordner und Ordner in Ordnern in einem Drop gehen. Ein Drop übernimmt höchstens 50 Dateien (`AppModel.maxDrop`), die ersten in Reihenfolge; bei mehr erscheint in der Leiste eine Info mit der Zahl der übernommenen Dateien, benannt nach dem Ordner, aus dem der Drop kam. Keine Rückfrage, kein Fehler: der Import beginnt sofort. Bewusst einfach: ein erneutes Ablegen desselben Ordners nimmt wieder dieselben ersten 50 (der Hash weist sie ab, importiert aber nichts Neues); die übrigen wählt der Nutzer einzeln aus. Sollte das stören, wäre der nächste Schritt, beim Drop bekannte Hashes vorab auszusortieren. Ohne App-Tests, das Limit ist eine Konstante im AppModel. Bewusst offen: ein Alias oder Symlink auf einen Ordner wird stillschweigend übergangen. Dedupe, Warteschlange und Parallelität dahinter sind unverändert. Der Löschdialog nennt jetzt, dass Belege ohne weitere Buchung mitgehen. Spec-Abschnitt Oberfläche ergänzt. 181 Tests, SwiftFormat und App-Build erfolgreich, unabhängig geprüft.

**Löschen nimmt verwaiste Belege mit (2026-09-21).** Eigentümerentscheidung nach dem Prinzip, dass Unsichtbares den Nutzer nicht blockieren darf: Löscht der Nutzer eine Buchung, entfernt `Repository.deleteWithReceipts` in derselben Transaktion die Zeilen in `dateien` für Belege, die keine andere Buchung mehr in `belege` trägt (Prüfung per `json_each` über die verbleibenden Buchungen), und gibt sie zurück; `AppModel.delete` löscht danach die Archivoriginale über `ArchivePaths.original`. Dieselbe Datei läuft beim nächsten Ablegen frisch, statt „bereits vorhanden“ zu melden. `Repository.delete` bleibt unverändert und räumt weiter die Buchungen eines gescheiterten Laufs auf, ohne die gespeicherte Datei anzutasten. Bewusst offen: Kontoauszüge trägt keine Buchung, sie bleiben bekannt; das Entfernen eines einzelnen Belegs aus einer Buchung lässt die Datei weiter stehen; `anfragen.datei_id` und `aktivitaeten.vorher` dürfen auf gelöschte Datei-IDs zeigen, nichts liest sie zusammen. Der Löschdialog nennt die Belege noch nicht. Spec-Ergänzung Dateien angepasst. 180 Tests, SwiftFormat und App-Build erfolgreich, unabhängig geprüft.

**Landingpage-Text und Neuaufbau (2026-09-21).** Der Text der Seite wurde getrennt von der Gestaltung in `landing-concepts/copy.md` erarbeitet und Sektion für Sektion mit dem Eigentümer abgestimmt; `daft.html` ist daraus frisch mit unverändertem DaftCSS gebaut, ohne eigenes CSS oder JavaScript. Aufbau: Hero mit Badge, Headline „Buchhaltung ohne Buchhaltung.“, Downloadbutton und Screenshot-Platzhalter; drei Karten (Ablegen, Steuer, Kontrolle); vier Text-Bild-Abschnitte entlang des Ablaufs (Belege mit Reverse Charge und Fremdwährung, Zahlungsabgleich, Startseite, Steuerexport mit AfA); neu ein Textabschnitt „Deine Buchhaltung bleibt bei dir“ (lokal, OpenAI extrahiert Angaben, Bestätigung jeder Buchung); „Passt Pfennig zu dir?“ als zweispaltige Tabelle „Das kann Pfennig“ gegen „Das ist nicht dabei“ statt der Beschränkung auf Freiberufler; „Was Pfennig kostet.“ als Pricing-Sektion mit zwei Karten, Abo zuerst (10 € im Monat, ab Version 1.0) und eigener Schlüssel (nach Nutzung, abgerechnet von OpenAI, bewusst kein „0 €“); FAQ ohne die Kostenfrage, dafür „Kann ich Pfennig ohne OpenAI nutzen?“; Schluss-CTA. Der Satz zum Aktivitätslog wurde gestrichen, weil die App das Log nicht anzeigt. Nach dem Vergleich mit einem frei gebauten Vanilla-Entwurf (`landing-concepts/opus.html`, nur Referenz) wurden in den Daft-Entwurf übernommen: Sticky-Navigation mit Glaseffekt und Popover-Menü auf Mobilgeräten, kleineres Apple-Symbol im Downloadbutton, größerer Abstand in den Text-Bild-Reihen, gedämpfte Absatzfarbe unter den Abschnittsüberschriften, zweispaltiges Layout (Überschrift links, Inhalt rechts) für „Deine Buchhaltung bleibt bei dir“ und die FAQ sowie ein Footer mit Logo. Aus dem zweiten, frei gebauten Opus-Entwurf (`landing-concepts/opus2.html`, Referenz) wurden Daft-nativ übernommen: „Aus Belegen werden Buchungen“ als Editorial mit Überschrift links, Text rechts und breitem Bild darunter; „Deine Zahlen sind bereit“ mit den zwei Absätzen nebeneinander und breitem Bild; die Passt-Tabelle als Karte mit Kapitälchen-Köpfen, Lucide-Haken links und grauen Kreuzen rechts; ein größerer Schluss-CTA mit Apple-Symbol. Bekannter Kompromiss: auf schmalen Bildschirmen presst die Tabelle die Symbole zusammen, weil Daft SVGs auf `max-width: 100%` setzt und es keine Utility gegen das Schrumpfen gibt; eine Zeile eigenes CSS oder eine vierspaltige Tabelle würde das lösen. Offen: die Tabellenzeilen in „Passt Pfennig zu dir?“ sind noch nicht vom Eigentümer bestätigt, Screenshots fehlen, das Downloadziel bleibt der GitHub-Release. Frühere Entwürfe und die Inhaltsvorschläge von Fable und Astra bleiben als Vergleich liegen. Keine Änderung an der macOS-App.

**Ruhiger Eingangsindikator.** Während der parallelen Dateiverarbeitung zeigt die Toolbar nur noch den Spinner. Der verzögert wirkende Textzähler und sein Fortschrittszustand sind entfernt; Sichtbarkeit und Verarbeitung bleiben unverändert.

**Validierung und Kleinunternehmer-Fristen.** `privatanteil_prozent` wird beim Agentenschreiben und Bestätigen auf 0 bis 100 begrenzt; Einnahmen eines Kleinunternehmers dürfen nicht als `inland` in die steuerpflichtigen UStVA-Kennzahlen gelangen. Die Startseite zeigt Kleinunternehmern keine regelmäßige UStVA-Frist mehr, wohl aber einen Monats- oder Quartalszeitraum, in dem die bestehende UStVA-Berechnung eine unterstützte §13b-Steuer in Kz 47 oder 85 ergibt. Regelbesteuerte Profile bleiben unverändert. 177 Tests und App-Build erfolgreich, unabhängig geprüft.

**Exportjahr-Schutz.** UStVA-XML und EÜR-CSV lassen sich nur für das Steuerjahr 2026 speichern. Die UStVA-Kennzahlen/XML-Struktur und die EÜR-Zuordnungen sind für andere Jahre nicht verifiziert; der Export bricht mit einer klaren deutschen Fehlermeldung ab, ohne eine Datei zu erzeugen. Die Berechnungen und das 2026-Verhalten bleiben unverändert; unabhängig geprüft.

**Dateien mit Pfennig öffnen, Version 0.7.2 vorbereitet (Build 9, 2026-09-20).** Pfennig ist für PDF, PNG, JPEG, WebP, XML, CSV, TXT, JSON und HTML als alternative Anwendung registriert. Finder und `open -a Pfennig …` liefern eine oder mehrere Dateien über den nativen AppKit-Öffnenweg; vor dem SwiftUI-Fenster eintreffende Dateien warten kurz auf dessen bestehendes `AppModel`, danach laufen sie unverändert durch denselben Eingang wie Drag-and-drop. Keine CLI, kein Dienst, kein externer API-Endpunkt, keine neue Importlogik und keine Standard-Dateizuordnung. Kein Release, Tag, Release-Asset oder Austausch der installierten App. Verifikation: 175 Tests, SwiftFormat, Property-List-Prüfung und App-Build erfolgreich; der gebaute Bundle trägt die zehn exakten Endungen mit Rang `Alternate`; unabhängiges Review nach zwei Korrekturen ohne weitere Beanstandung.

**Version 0.7.1 vorbereitet (Build 8, 2026-09-20).** Die Versionsnummer dokumentiert die Mehrfachauswahl und gemeinsame Löschung von Buchungen. Kein Release, Tag, Release-Asset oder Austausch der installierten App.

**Mehrfachauswahl in Buchungen (2026-09-20).** Die native Tabellenwahl nimmt jetzt mehrere Buchungen per Befehlsklick oder Umschaltklick an. Bei genau einer Auswahl bleibt der Inspector unverändert; bei mehreren zeigt seine offene Seitenleiste nur deren Anzahl und bietet bewusst keine Sammelbearbeitung. Löschtaste und Kontextmenü löschen die gesamte Auswahl nach einer passend formulierten Bestätigung. Das Repository entfernt alle gewählten Buchungen in einer Transaktion und protokolliert jede einzeln. Keine Schemaänderung. Verifikation: 175 Tests, SwiftFormat und App-Build erfolgreich; unabhängiges Review ohne Beanstandungen.

**Importabschluss und optionaler Akzeptanztest (2026-09-20).** Der Abschlussstatus in `anfragen` wird getrennt von Tokenzahlen und Konversation gespeichert. Nur ein dauerhaft gespeicherter Erfolg erlaubt das Entfernen der Inbox-Kopie; ein Diagnosefehler ändert diesen Erfolg nicht. Scheitert der Abschluss, bleiben Inbox und bereits geschriebene Buchungen erhalten. Keine Schemaänderung. Regressionen prüfen beide Fehlerpfade, Deduplizierung ohne Diagnose und den erneuten Versuch nach gescheitertem Abschluss. `scripts/test-live.sh` aktiviert einen standardmäßig ausgelassenen Test mit synthetischer PDF-Rechnung, CSV-Zahlungszuordnung und Doppelimport gegen Luna mit niedrigem Aufwand; ausschließlich temporäres Archiv. Der Live-Lauf wurde nicht ausgeführt. Verifikation: Offline-Suite mit 174 Tests erfolgreich (Live-Test ausgelassen), ergänzter Retry-Test erfolgreich, SwiftFormat und App-Build erfolgreich. Unabhängige Reviews der Dokumentation und der Implementierung ohne Beanstandungen. Die Bereinigung gemeinsamer Buchungsaktionen bleibt für eine spätere Änderung zurückgestellt.

**Rebuild-Spezifikation entfernt (2026-09-20).** Auf Entscheidung des Eigentümers ist die initiale Spezifikation aus dem Arbeitsbaum entfernt. Produktgrenzen und Arbeitsregeln stehen in `AGENTS.md`; neue Arbeit wird im Gespräch vereinbart, ohne Spec-Pflege. Ältere Einträge hier sind Entwicklungshistorie, keine verbindlichen Vorgaben. Die damalige Spezifikation bleibt in Git erhalten.

**Tests vor dem Release, Werkzeugbeschreibung richtiggestellt (2026-09-20).** `scripts/release.sh` ruft `swift test` auf, bevor es das Archiv entfernt; eine rote Suite lässt damit das vorige Release unberührt. Der Satz `SQLTool.allowed`, den Fehlermeldung und Werkzeugbeschreibung gemeinsam benutzen, nannte nur `buchungen`, obwohl der Autorisierer `afa_tabelle` lesend freigibt und `Repository.schemaText` dem Agenten deren Schema ohnehin zeigt. Der Agent bekam damit zwei widersprüchliche Angaben: die Regel in der Anleitung, die Nutzungsdauer aus `afa_tabelle` zu holen, und eine Werkzeugbeschreibung, die das ausschloss. Der Satz nennt jetzt beides getrennt. Nebenwirkung des neuen `swift test` im Release: SwiftPM und `xcodebuild` schreiben `Package.resolved` unterschiedlich, SwiftPM ohne den Sparkle-Pin, `xcodebuild` mit ihm. Eingecheckt ist die Form von `xcodebuild`, weil sie die ausgelieferte App beschreibt; ein Releaselauf dreht die Datei zwischendurch und am Ende wieder zurück.

**Prüfregeln nach Adressat getrennt (2026-09-20).** `ValidationRules.alle` enthält nur noch die Regeln, die eine Buchung ungültig machen; `steuerPasstZumSatz` und `datumLiegtNichtWeitInDerZukunft` stehen daneben als `leseregeln` und laufen allein im `sql`-Werkzeug, wo ein Rückweisen den Agenten das Dokument im selben Lauf noch einmal lesen lässt. Beim Bestätigen hielten die beiden den Nutzer davon ab, etwas Wahres festzuhalten: eine mehrzeilige Rechnung, deren Rundung je Zeile um mehr als einen Cent von der Rechnung aus der Summe abweicht, oder eine echt vorausdatierte Rechnung; der einzige Ausweg war Löschen und Neutippen, das jede Prüfung umgeht. Dazu gilt `inlandNurMit19Oder7` mit den Sätzen 19 und 7 nur noch für Einnahmen, weil das Formular 2026 nur dafür Zeilen hat: eine Eingangsrechnung mit dem Pauschalsatz nach §24 UStG ist zulässig und geht über die ausgewiesene Steuer richtig in Kz 66 ein. Steuersatz 0 gehört weiterhin in beiden Richtungen auf steuerfrei oder nicht_steuerbar. Die Meldung von `steuerPasstZumSatz` nennt nicht mehr den erwarteten Cent-Betrag, den der Agent sonst einfach übernimmt, sondern schickt ihn zurück in den Beleg. Die Zuordnung `case 0: 87` in `Kennzahl.einnahme` bleibt stehen: sie ist nur für den Agenten tot, denn über den Inspector lässt sich eine ungeprüfte Inlandseinnahme mit Satz 0 speichern, und die UStVA rechnet ungeprüfte Buchungen mit; ein Entfernen verlöre die Bemessung stillschweigend.

**Zehn-Prozent-Grenze der Vorsteuer entfällt (2026-09-20).** Die Grenze des §15 Abs. 1 Satz 2 UStG, die den Vorsteuerabzug unter zehn Prozent betrieblicher Nutzung ganz strich, gilt nur für Gegenstände; für sonstige Leistungen gilt das Aufteilungsgebot, und auch ein kleiner betrieblicher Anteil bleibt abziehbar. Ob eine Buchung einen Gegenstand oder eine Leistung betrifft, weiß Pfennig nicht, und die Entscheidung des Eigentümers ist, die Grenze zu streichen: falsch bleibt sie damit nur noch bei Gegenständen unter zehn Prozent betrieblicher Nutzung, die praktisch nicht gebucht werden. Kz 66 und die EÜR-Zeile 58 nehmen jetzt durchgehend den betrieblichen Anteil; Kz 67 tat das schon vorher. In der EÜR wandert damit bei solchen Buchungen der Steueranteil von der Kategoriezeile auf Zeile 58, der Gewinn bleibt gleich. `UStVA.abziehbar` war danach nur noch eine Weiterleitung an `EUeR.ohnePrivatanteil` und ist entfallen; beide Aufrufstellen rufen direkt. 172 Tests grün.

**Release 0.6.0 veröffentlicht (Build 6, 2026-09-18).** Enthält die UStVA-Korrekturen (Kz 21 zum Belegdatum, §13b je Positionssatz, Kz 45 nur ins Ausland), Anlagevermögen mit AfA-Tabelle und Anlage AVEÜR, die EÜR auf dem amtlichen Formular 2026, Dateien zuerst mit vom Agenten gepflegten Belegen, Kontoauszüge in der Anleitung, Textformate im Eingang, den HTTP/3-Fix sowie die sieben Review-Korrekturen aus dem Release-Review und die Bestätigungsprüfung. Auf die Freigabe des Eigentümers lief `scripts/release.sh --notarize` mit SPARKLE_BIN aus dem offiziellen Sparkle-2.10.0-Archiv durch; Tag `v0.6.0` auf `60ff402`, `main` und Tag gepusht, alle sechs Assets als Latest-Release veröffentlicht. Unauthentifizierte Downloads aller Assets und des Latest-Appcasts stimmen byteweise mit `dist/` überein. `/Applications/Pfennig.app` bleibt auf 0.5.0; da das Nutzerarchiv bereits das neue Schema trägt, sollte der Eigentümer zeitnah über Sparkle aktualisieren, die installierte 0.5.0 darf es nicht öffnen.

**Steuer nach dem Wechsel von Reverse Charge (2026-09-18).** Der Wechsel weg von `reverse_charge` rechnet die Positionssteuer aus Netto und Satz neu, wie eine Netto- oder Satzänderung; vorher blieb Brutto gleich Netto, bis die Bestätigung die Buchung zurückwies. Der Wechsel auf Reverse Charge leert weiter, das Steuerfeld bleibt deaktiviert. Die Regression ergänzt den Reverse-Charge-Formattest.

**Anführungszeichen im EÜR-CSV (2026-09-18).** Der EÜR-Export setzt alle Textzellen in doppelte Anführungszeichen und verdoppelt ein eingebettetes Anführungszeichen, Zahlen und Daten bleiben unquotiert. Vorher stand `anlage.titel` roh in der Zeile des Anlageverzeichnisses; ein Semikolon im Titel verschob Spalten, ein Zeilenumbruch fügte Zeilen hinzu. Regressionen decken Titel mit Semikolon, Anführungszeichen und Zeilenumbruch ab, jeweils mit intaktem Zeilenaufbau. 169 Tests, SwiftFormat und App-Build erfolgreich.

**Inbox-Kopie bereits vorhandener Dateien (2026-09-18).** Der Eingang entfernt im Befund „bereits vorhanden“ jetzt die Inbox-Kopie einer Datei, für die ein Lauf gelungen ist (nur innerhalb der Inbox, ein Fehlschlag beim Entfernen wird toleriert); ein Original von außerhalb bleibt unberührt. Der Fall entstand, wenn nach einem gescheiterten Lauf ein erneutes Ablegen gelang: die Kopie des Fehlversuchs blieb liegen und meldete sich bei jedem Start von neuem, ohne Verwerfen-Möglichkeit. Regressionen decken das Wegräumen der Inbox-Kopie und das unberührte Original ab.

**EÜR für ungekategorisierte Anlagegüter.** Die EÜR nimmt ein Anlagegut auf der Ausgabenseite auch ohne Kategorie auf: Die AfA steht auf Zeile 34, die abziehbare Vorsteuer bleibt im Zahlungsjahr auf Zeile 58, und das Anlagegut bleibt in der Anlage AVEÜR. Gewöhnliche Buchungen ohne Kategorie bleiben ausgeschlossen. Regressionen decken Anschaffungsjahr, Folgejahr und AVEÜR-Zugehörigkeit ab. Zusammen mit der Zeitraum-Korrektur: 165 Tests und App-Build erfolgreich, unabhängig geprüft.

**AfA-Jahre in gemeinsamen Zeitraum-Hinweisen (2026-09-17).** `Zeitraum.beruehrt` nutzt für EÜR-Jahre zusätzlich den bestehenden `AfA.betrag`-Zeitplan: ein Anlagegut berührt jedes aktive AfA-Jahr einschließlich des letzten Teiljahrs, aber weder Jahre davor noch danach. Dadurch erreichen spätere AfA-Jahre ohne Sonderlogik `Start.fristen`, den Hinweis auf ungeprüfte Buchungen und `AppModel.changedAfterExport`; UStVA sowie die bisherige Beleg- und Zahlungszuordnung bleiben unverändert. Einjahresgüter bleiben auf das Anschaffungsjahr begrenzt. 165 Tests, SwiftFormat und App-Build erfolgreich.

**Privatanteil bei Reverse-Charge-Dienstleistungen.** Kz 67 nimmt über den bestehenden Privatanteil nur den betrieblichen Anteil der berechneten Steuer auf; Bemessungsgrundlage und geschuldete Steuer in Kz 46/47 bzw. 84/85 bleiben unverändert. Die Zehn-Prozent-Grenze wird hier nicht angewandt: §15 Abs. 1 Nr. 4 UStG erlaubt den betrieblichen Abzug bei §13b-Leistungen, während Satz 2 ausdrücklich Gegenstände betrifft. Kleinunternehmer erhalten weiterhin keinen Kz-67-Abzug. Tests decken 0, 40, 95 und 100 Prozent Privatanteil bei 19 und 7 Prozent sowie Kleinunternehmer ab. Beide Review-Korrekturen gemeinsam: 159 Tests und App-Build erfolgreich, unabhängig geprüft.

**Bestätigung prüft gespeicherte Buchung (2026-09-17).** Die Bestätigen-Aktion prüft die aktuelle persistierte Buchung mit `ValidationRules` und dem aktuellen Profil in derselben Repository-Transaktion; bei Fehlern bleiben Prüfzeitpunkt und Aktivitätslog unverändert, und die bestehende Fehlermeldung zeigt die Regeltexte. Unvollständige Entwürfe werden weiterhin wie bisher automatisch gespeichert. Wechselt ein Anlagegut im Inspector von Ausgabe zu Einnahme, leert die Richtungsänderung `nutzungsdauer_jahre`, statt den Wert verborgen weiterzuführen.

**Reverse-Charge-Bearbeitung im Inspector.** Bei `reverse_charge` bleiben Position.steuersatz und damit der anwendbare Satz erhalten, während `Position.steuer` bei Netto- und Satzänderungen null bleibt. Beim Wechsel auf Reverse Charge leert der Inspector vorhandene Positionssteuer; das Steuerfeld ist dann deaktiviert. Andere Behandlungen behalten die bisherige Neuberechnung und direkte Bearbeitung. Die Regressionen stehen in den Core-Formattests.

**Drei UStVA-Korrekturen (2026-09-17).** Issues 9, 10 und 13, jede klein und am amtlichen Text belegt. Kz 21 folgt jetzt der Leistungsausführung statt dem Geld: `UStVA.einnahme` bucht eine Reverse-Charge-Einnahme an einen EU-Staat mit dem vollen Netto in den Zeitraum des Belegdatums und sieht die Zahlungen gar nicht mehr an (Anleitung USt 1 E 2026 zu Zeile 35, §18b Satz 3 UStG); das Belegdatum ist die Näherung für den Tag der Leistung, ein Leistungsdatum gibt es weiter nicht. Kz 45 auf der Einnahmenseite bleibt bewusst bei der Zahlung, dafür fand sich keine Quelle, die etwas anderes verlangt. §13b kennt jetzt beide Sätze: eine Position mit `reverse_charge` trägt den Satz, den der Leistungsempfänger schuldet (19 oder 7), und `steuer` 0, `UStVA.ausgabe` rechnet Kz 47, 85 und 67 je Position aus diesem Satz statt pauschal mit 19 Prozent. Dafür prüft `steuerPasstZumSatz` die Positionen einer Reverse-Charge-Buchung nicht mehr (sie tragen keine berechnete Steuer, sondern den geschuldeten Satz), und die neue Regel `reverseChargeAusgabeBrauchtSatz` verlangt 19 oder 7 bei einem Bezug; bei einer eigenen Leistung ins Ausland greift sie nicht, weil dort der Empfänger seinen eigenen Satz schuldet und das Formular ihn nie fragt. Der Schemakommentar zu `positionen` sagt die Konvention an; bestehende Datenbanken sehen ihn erst nach einer Neuanlage. Kz 45 nimmt außerdem nur noch Umsätze mit `gegenpartei_land` ungleich DE auf, nicht steuerbare Inlandsumsätze bleiben draußen (Anleitung zu Zeile 36); die Ausnahme über `art = steuerzahlung` in `UStVA.zaehlt` steht unverändert. 151 Tests grün, App-Build grün.

**Anlagevermögen und AfA (2026-09-17).** Die Ergänzung Anlagevermögen der Spec ist gebaut. Ein Anlagegut ist die Kaufbuchung selbst: `buchungen.nutzungsdauer_jahre` (leer heißt gewöhnliche Ausgabe) macht sie dazu, eine Prüfregel lässt die Spalte nur bei Ausgaben und größer null zu, sonst zwingt nichts. Die Nutzungsdauer ist Wissen, keine Regel: die amtliche AfA-Tabelle (BMF vom 15.12.2000, Computerhardware und Peripherie mit einem Jahr nach BMF vom 22.02.2022) liegt als `Sources/Core/Resources/afa_tabelle.csv` im Bundle, steht als Tabelle `afa_tabelle` im Schema, wird bei jedem Öffnen der Datenbank gelöscht und neu eingefügt (`Wissen.einspielen` im selben Schreibvorgang wie `Schema.create`) und ist für den Agenten lesbar, nicht schreibbar; damit steht ihr CREATE auch in der Anleitung, und ein Update bringt korrigierte Zeilen ohne Migration mit. Die Anleitung bekam einen Satz dazu. `Sources/Core/AfA.swift` rechnet linear: Anschaffungskosten sind das Netto ohne Privatanteil (beim Kleinunternehmer das Brutto), Jahresbetrag Kosten durch Jahre, das Anschaffungsjahr anteilig ab dem Anschaffungsmonat, ein Jahr Nutzungsdauer voll im Anschaffungsjahr. Alle drei Funktionen sind Differenzen einer kumulierten AfA, die bei den Anschaffungskosten endet, deshalb ist die Summe über alle Jahre exakt gleich den Kosten und der Rest fällt in das Jahr, das sie erreicht: ein Schreibtisch vom 15.03.2026 über 13 Jahre schreibt 2026 nur 192,31 ab und deshalb 2039 noch 38,45. In der EÜR steht ein Anlagegut nicht mehr auf seiner Kategoriezeile, sondern mit der AfA des Jahres auf Zeile 34, unabhängig von der Zahlung (§4 Abs. 3 Satz 3 EStG); seine Vorsteuer bleibt unverändert auf Zeile 58 im Zahlungszeitraum. Die CSV bekommt, sobald es Anlagegüter gibt, einen zweiten Block: die Zeilen 48 bis 54 und 63 der Anlage AVEÜR für die Gruppe Büroausstattung, darunter je ein Anlagegut mit Titel, Anschaffungsdatum, Kosten, AfA und Restbuchwert, und eine Fußzeile mit dem, was nicht abgebildet ist. Der Inspector zeigt bei Ausgaben „Nutzungsdauer in Jahren“; die Tabelle ändert sich nicht. 148 Tests grün, App-Build grün. Bewusst offen und in der Fußzeile des Exports genannt: Fahrzeuge, Gebäude und Grundstücke, immaterielle Wirtschaftsgüter und Software als Anlage, Verkauf und Privatentnahme (Zeile 39, Zeile 19), degressive AfA und §7g, Sammelposten, nachträgliche Anschaffungskosten. Ebenfalls dokumentierte Lücken: die Nutzungsentnahme bei einem Anlagegut mit Privatanteil (es geht nur mit dem betrieblichen Anteil ein) und die nicht abziehbare Vorsteuer, die nach §9b EStG zu den Anschaffungskosten gehörte, hier aber der Spec folgend außen vor bleibt. Die Datenbank des Eigentümers braucht die Spalte und die Tabelle von Hand, danach füllt der nächste Start die Zeilen.

**Vorsteuer und Bewirtung in der EÜR korrigiert (2026-09-17).** Zwei Befunde aus Issue 14 und 15, beide klein und im Formular belegt. Zeile 58 nimmt nur noch die nach §15 UStG abziehbare Vorsteuer: `inland` ja, alles andere (ausländische Steuer, steuerfrei, nicht steuerbar, §13b) nein, und unter zehn Prozent betrieblicher Nutzung nichts. Was nicht abziehbar ist, bleibt bei der Ausgabe und steht brutto auf ihrer Kategoriezeile, wie die Anleitung zu Zeile 58 es verlangt; bisher verschwand dieser Teil aus dem Gewinn. Die Arithmetik je Ausgabe ist `ohnePrivatanteil(netto) + ohnePrivatanteil(steuer) - abziehbar`, für den Regelfall also unverändert das Netto des betrieblichen Anteils. Zeile 64 hat im Formular zwei Spalten (Kz 165 nicht abziehbar, Kz 175 abziehbar); Bewirtung wird deshalb auf zwei `EUeR.Zeile` mit derselben Nummer aufgeteilt, 30 Prozent nicht abziehbar zuerst, dann 70 Prozent abziehbar, und nur die 70 Prozent zählen in `ausgaben` und `ergebnis` (Flag `nichtAbziehbar`, die `id` einer Zeile ist jetzt Nummer plus Bezeichnung). Die Vorsteuer der Bewirtung bleibt voll abziehbar, so will es die Anleitung. 139 Tests grün, App-Build grün. Weiter offen aus dem EÜR-Bereich: Wertabgaben (Zeile 19 bis 21) und die AfA für Anschaffungen über 800 Euro (Issue 1); beides bewusst nicht angefasst.

**EÜR-Zeilen auf die Anlage EÜR 2026 gebracht (2026-09-17).** Eine Sonnet-Recherche zur EÜR-Übergabe fand, dass unsere Zeilennummern ein alter Platzhalter waren; erst gegen das Formular 2025 neu zugeordnet, dann auf Eigentümerentscheidung allein auf das amtliche Formular 2026 (BMF-Schreiben vom 01.09.2026, PDF ausgelesen). Das Formular 2026 hat dieselben Einnahmezeilen, schiebt aber mit der neuen Zeile 27 jede Ausgabezeile ab der alten 29 um eins nach hinten. Einnahmen folgen der Steuerbehandlung, nicht der Kategorie: Kleinunternehmer alles auf 12, steuerpflichtig 15, steuerfrei/nicht steuerbar/§13b 16, Erstattung 18; vereinnahmte Umsatzsteuer 17, Vorsteuer 58, Umsatzsteuerzahlung 59. Ausgaben: Fremdleistung 30, Hardware 37 (GWG, richtig nur unter 800 Euro, darüber braucht es die AfA aus Issue 1), Miete 40, Telekommunikation 44, Übernachtung 45, Fortbildung 46, Beratung 47, Beiträge/Gebühren/Versicherung 50, Software und Hosting 51, Bürobedarf und Porto 52, Werbung 55, Sonstiges 61, Bewirtung 64 (das Formular hat dort zwei Spalten, wir liefern den vollen Betrag, die 70-Prozent-Kürzung bleibt offen), Fahrtkosten 71. Der Export trägt die amtlichen Zeilentitel; das Formularjahr steht einmal als `EUeR.formularjahr` und erscheint in der CSV-Kopfzeile, damit die Nummern zuordenbar sind. Dabei zwei kleine Korrekturen: ein Privatanteil kürzt in der EÜR nur noch Ausgaben, vorher hätte er bei einer Einnahme den Umsatz verkleinert; Griechenland zählt jetzt auch unter der umsatzsteuerlichen Kennung `EL` als EU-Mitgliedstaat, nicht nur unter ISO `GR`. Abschnitt 4 von `docs/research-ustva-xml.md` beschrieb Kz 46/47 und 84/85 als verschiedene §13b-Tatbestände; er ist auf den Code korrigiert, der Recht hat: 46 Bemessung und 47 Steuer für Leistungen aus dem EU-Ausland, 84/85 für alles übrige, 67 die zugehörige Vorsteuer. Ergebnis der Formatrecherche: Es gibt keinen Upload für die Anlage EÜR in Mein ELSTER und kein öffentliches Importformat bei WISO Steuer; CSV zum Abtippen bleibt der Weg, wie bei sevDesk und Lexoffice. Außerdem steht im Systemprompt jetzt der Satz, dass Dateiinhalt Daten sind und keine Anweisung. Offene Punkte aus den Recherchen liegen als GitHub-Issues 1 bis 8.

**Fünf Steuerkorrekturen (2026-09-17).** Ein Opus-Review hatte fünf offene Fragen aus dem Code-Review steuerlich geprüft; der Eigentümer hat die minimale Umsetzung freigegeben. Gebaut: Kz 66 und die EÜR-Zeile „Gezahlte Vorsteuer“ nehmen nur den betrieblichen Anteil der Vorsteuer und unter zehn Prozent Nutzung nichts (`UStVA.abziehbar`, §15 Abs. 1 Satz 2 UStG; die EÜR-Seite fand das Sonnet-Review); zwei neue Prüfregeln `inlandNurMit19Oder7` und `reverseChargeOhneSteuer` ersetzen `inlandOhneSteuerBrauchtEigeneBehandlung`; `Zeitraum.frist` rückt über `Werktag.amOderNach` auf den nächsten Werktag, mit den neun bundesweiten Feiertagen und einer Osterformel (§108 Abs. 3 AO), regionale Feiertage bewusst nicht; `Repository.delete` schreibt eine Aktivität mit `vorher` und leerem `nachher`, dafür ist `aktivitaeten.nachher` nullbar. 132 Tests grün, App-Build grün. Bewusst nicht gebaut und im Backlog: Wertabgaben, Kz 87, Kleinunternehmer-Hinweis bei §13b, Bundesland im Profil, Storno. Die lokale Datenbank des Eigentümers braucht die eine Spaltenänderung von Hand (Tabelle `aktivitaeten` neu anlegen, Zeilen übernehmen).

**HTTP/3-Fehler beim Eingang (2026-09-16, Abend).** Zehn gleichzeitig abgelegte PDFs scheiterten mit dem Socket-Fehler EMSGSIZE („Die Nachricht ist zu lang“). Reproduziert ohne Pfennig mit einem Testskript: Sobald macOS für api.openai.com HTTP/3 kennt (Alt-Svc, systemweit gelernt), scheitern gleichzeitige große Uploads über QUIC zu zehn aus zehn; über HTTP/2 gehen alle durch. Nach Fehlschlägen stuft das System HTTP/3 für den Host eine Weile zurück, deshalb lief der elfte Lauf durch. Der Eintrag liegt in `~/Library/HTTPStorages/com.pietz.pfennig/httpstorages.sqlite`, Tabelle `alt_services`. Ein Abschalten von HTTP/3 bietet URLSession öffentlich nicht, aber eine ephemere Session liest diesen Speicher nicht und bleibt bei HTTP/2; deterministisch gemessen in einer HTTP/3-Phase (zehn frische ephemere Sessions: 10 von 10 über HTTP/2, eine geteilte Standard-Session: 10 von 10 EMSGSIZE über HTTP/3). `Responses.network` erzeugt deshalb je Anfrage eine ephemere Session. Ein Opus-Review des Diffs gegen 0.5.0 bestätigte, dass Anzahl, Größe und Zeitpunkt der Anfragen unverändert sind. Zusätzlich wiederholt `Responses.send` einen Netzfehler einmal nach drei Sekunden; die Dublettenprüfung gilt erst nach gelungenem Lauf, damit gescheiterte gespeicherte Dateien erneut laufen können. Offen: Live-API-Tests mit Luna, siehe Backlog.

**Dateien zuerst, Belege vom Agenten (2026-09-16).** Der Eingang speichert eine Datei vor dem Lauf: Archivkopie und Zeile in `dateien` mit hochzählender `id` und eindeutigem `sha256`; `art` ist weg, ob eine Datei Beleg ist, sagt `belege`. Der Agent schreibt `belege` als Liste von Datei-IDs selbst, das SQL-Werkzeug prüft sie gegen `dateien`. Swift hängt nichts mehr an und löscht keine Dateien mehr; `saveFileAndAttachReceipt`, `receiptIsUsed`, `cleanupOrphanedFiles` und `ArchivePaths.remove` sind weg. Dedupe ist wieder „Hash existiert“, die Inbox-Kopie eines gescheiterten Laufs ist die Ausnahme und verwendet die gespeicherte Datei. Die Anleitung kennt Kontoauszüge, `maxToolCalls` ist 60. Datenbank und Archiv des Eigentümers wurden nach `Backups/pfennig-pre-dateien-id-*` gesichert und zurückgesetzt; die installierte 0.5.0 darf mit dem neuen Archiv nicht mehr gestartet werden, bis ein Release folgt.

**Textformate im Eingang (2026-09-16).** `FileInput` kennt zwei Endungslisten: Binärformate (PDF, PNG, JPEG, WebP) gehen als Anhang, Textformate (XML, CSV, TXT, JSON, HTML) als Klartext bis 1 MB, UTF-8 strikt mit Windows-1252 als Rückfall. Kein Parser je Format; XRechnungen liest der Agent als Text. Zwei KoSIT-Musterrechnungen liegen als Fixtures unter `Tests/AgentTests/Fixtures` mit Quellenangabe.

**Review und Backlog (2026-09-16).** Vollständiges Code-Review nach 0.5.0; sechs Befunde behoben (Kz 45 für Reverse-Charge-Einnahmen ins Drittland, Protokollfehler bricht keinen Import mehr ab, Drag-and-drop auf Start, Warteschlange, Schlüsselbund-Status, Statusfilter bei „Neue Buchung“). Offene Entscheidungen aus dem Review: Privatanteil in der UStVA, Form von Reverse-Charge-Positionen, Inlandsumsätze mit anderem Satz, Wochenendverschiebung von Fristen, Aktivität beim Löschen. Produktideen und empfohlene Reihenfolge stehen in `docs/backlog.md`.

**Release 0.5.0 veröffentlicht und installiert (Build 5, 2026-09-16).** Enthält Startseite, vereinfachte Zahlungen, Überfällig-Filter, Bezahlt-Korrektur, Betriebsnutzungs-Regel und kompaktes Fenster. Auf ausdrückliche Freigabe des Eigentümers: `scripts/release.sh --notarize` durchgelaufen, Prüfsummen und Appcast (Version 5, 0.5.0) geprüft, Tag `v0.5.0` auf `31694bb`, `main` und Tag gepusht, alle sechs Assets als Latest-Release veröffentlicht. Unauthentifizierte Downloads aller Assets und des Latest-Appcasts stimmen byteweise mit `dist/` überein. `/Applications/Pfennig.app` durch das notarisierte ZIP ersetzt; codesign, Gatekeeper und Stapler bestätigt, App läuft auf dem umgestellten Archiv. Die vorherige 0.4.0 liegt unter `Backups/app-before-0.5.0-20260916-173437/`. Beim Installieren war ein Fehlgriff dabei: der ZIP entpackt in `Pfennig-0.5.0/`, der erste Kopierversuch scheiterte und ließ kurz ein leeres Bundle zurück; korrigiert, bevor etwas gestartet wurde. Der Sparkle-Aktualisierungszyklus 0.4.0 auf 0.5.0 wurde damit nicht getestet.

**Startseite (2026-09-16).** Die Seitenleiste hat jetzt „Start“ und „Buchungen“; die App öffnet auf Start. Die Seite ist vollständig aus den Buchungen abgeleitet und hält keinen eigenen Zustand. `Sources/Core/Start.swift` liefert die drei To-Do-Gruppen, die offenen Fristen und die Jahreswerte als reine Funktionen. Die Anzahl jeder To-Do-Zeile wird mit demselben `ReviewFilter` gezählt, auf den der Klick führt, damit Zahl und Liste nicht auseinanderlaufen; Prüfen und Belege überschneiden sich daher wie die Filter. Neu ist `ReviewFilter.ueberfaellig` über `Buchung.istUeberfaellig`. Die Jahreswerte zählen nach Zahlungsdatum, nicht nach Belegdatum, weil die Steuer es auch so tut. Das Jahr ist über ein Klappmenü oben rechts wählbar; es gilt trotz dieser Position nur für die drei Kacheln, weil To-Dos und Fristen den heutigen Stand beschreiben und kein Steuerjahr kennen. Die Fristenliste enthält immer den nächsten UStVA-Zeitraum und das nächste EÜR-Jahr, frühere Zeiträume nur, wenn eine Buchung in sie fällt, ohne die exportierten, nach Frist sortiert; eine leere Datenbank zeigt so nichts als überfällig; die Karte zeigt davon die drei dringendsten. Die Obergrenze sitzt in der Ansicht, nicht in `Start.fristen`, weil sie eine Frage des Layouts ist. Das Export-Sheet nimmt jetzt einen vorausgewählten Zeitraum. Die Toolbar ist seitenabhängig; „Neue Buchung“ und „Export“ bleiben auf Start, damit die Titelleiste beim Seitenwechsel nicht zusammenfällt.

Zwei Korrekturen dabei: `Buchung.zahlungsstand` gibt bei Brutto null jetzt „Bezahlt“ statt „Offen“ zurück, denn eine Buchung ohne zu zahlenden Betrag konnte vorher nie bezahlt werden und der Klick in der Bezahlt-Spalte war wirkungslos. `UStVA.geordnet` bricht den Gleichstand gleicher Zahlungsdaten jetzt über den Listenindex; der vorherige Kommentar berief sich auf eine Sortierstabilität, die Swift nicht zusagt. Ohne Wirkung auf die Ergebnisse, weil Zahlungen desselben Tages immer im selben Zeitraum liegen. Verifikation: 124 Tests grün, App-Build grün, SwiftFormat sauber; unabhängiges Review freigegeben, seine Befunde (Export-Zeitraum in der Buchungs-Toolbar nicht zurückgesetzt, leere Zeiträume vor der ersten Buchung, Spec-Widersprüche zur Jahresauswahl) sind eingearbeitet.

**Entwicklungsarchiv auf das neue Zahlungsformat umgestellt und 0.5.0 vorbereitet (2026-09-16).** Auf Eigentümerfreigabe wurde die Datenbank nach Beenden der App aus der einzigen Schemadefinition neu erzeugt und alle Zeilen aus der alten Datei übernommen: 11 Buchungen, 11 Dateien, 50 Aktivitäten, 22 Anfragen, Einstellungen, Zeiträume und Sequenz. Im Zahlungs-JSON wurden die alten Schlüssel `id`, `richtung` und `geprueft` entfernt; da keine Erstattung vorlag, blieben alle Beträge positiv und damit korrekt. Kein Reimport, Prüfstatus und Belege erhalten. Alte Datenbank und Archivoriginale liegen unter `~/Library/Application Support/Pfennig/Backups/pfennig-pre-0.5.0-20260916-172916/`. Bis 0.5.0 installiert ist, darf `/Applications/Pfennig.app` (0.4.0) dieses Archiv nicht öffnen. Version auf 0.5.0, Build 5, `CHANGELOG.md` ergänzt. Vorgemerkt als To-Do: ein eigener Archivordner für den Debug-Build (`Pfennig-Dev`) mit erfundenen Testdaten, damit Überfälliges, Teilzahlungen und Fristen auf der Startseite überhaupt vorkommen.

Offen und bewusst nicht gebaut: kein Leistungsdatum, keine Dokumente-Seite, keine Umbenennung von „Buchungen“ zu „Vorgänge“, keine Statistiken, kein Ausblenden einzelner Punkte, kein Marker „kein Beleg nötig“ (unnötig, weil `reviewStatus` einen Beleg nur bei Rechnung, Beleg und Gutschrift verlangt).

**Zahlungen vereinfacht (noch nicht veröffentlicht).** 110 Tests, SwiftFormat und App-Build erfolgreich; unabhängiges Review `aee7a335` freigegeben. Netto 50 Produktionszeilen entfernt. `zahlungen` ist eine Liste nur aus Datum und vorzeichenbehaftetem EUR-Cent-Betrag relativ zur Buchung: positiv für eine Zahlung, negativ für eine Erstattung. Eigene ids, Richtung und Prüfung pro Zahlung entfallen; geprüft wird ausschließlich die gesamte Buchung über `geprueft_am`, entsprechend warnt der Export nur vor ungeprüften Buchungen. Der abgeleitete Zahlungsstand ist binär und berücksichtigt auch negative Gutschriften: unvollständig bleibt „Offen“, vollständig oder darüber hinaus bezahlt ist „Bezahlt“. Die Entwicklungsdatenbank wurde nicht neu aufgebaut und ist mit diesem Stand inhaltlich inkompatibel: alte Erstattungen tragen einen positiven Betrag mit Gegenrichtung, den das neue Modell als positive Zahlung lesen würde; außerdem zeigt ihr `sqlite_master` noch den alten JSON-Kommentar. Der neue Build darf deshalb nicht mit dem alten Archiv geöffnet werden. Vor dem ersten Start sind ein koordinierter Backup-, Neuaufbau- und Reimport-Schritt nach der Pre-1.0-Regel erforderlich; eine Kompatibilitätsschicht ist ausdrücklich nicht vorgesehen.

**Kompakte Startansicht (noch nicht veröffentlicht).** Standardfenstergröße 840 × 500 Punkte; der rechte Inspector startet ausgeblendet und bleibt über den bestehenden Toolbar-Schalter erreichbar. Zwei geänderte Vorgabewerte, keine zusätzliche Fensterverwaltung. Die native Wiederherstellung einer vorherigen Fenstergröße bleibt unverändert. SwiftFormat, App-Build und unabhängiges Review `00f0b450` erfolgreich.

**Release 0.4.0 veröffentlicht (Build 4, 2026-09-16).** Enthält Belegnummer/Fälligkeit und den minimalen Agentenkontext. 109 Tests, SwiftFormat, App-Build und Release-Vertragsprüfung erfolgreich; Review `76e281e4` freigegeben. Produktionscode des Prompt-/Zugriffsumbaues netto 85 Zeilen weniger. Der Eigentümer hat den Signier- und Notarisierungslauf ausgeführt. Nach öffentlicher Signaturprüfung, Stapler/Gatekeeper-Prüfung und Prüfsummenabgleich wurden auf seine ausdrückliche Release-Freigabe alle sechs Assets zusammen unter `v0.4.0` veröffentlicht; Tag zeigt auf `dcee8b0`. Unauthentifizierte Downloads aller Assets und des Latest-Appcasts stimmen byteweise mit den geprüften Dateien überein. Keine weitere Schemaänderung und kein Zurücksetzen der aktuellen Buchungen. Installation übernimmt der Eigentümer über den eingebauten Sparkle-Updater, um den offiziellen Aktualisierungsweg zu testen. Die vorherige Beenden-Anfrage wurde abgebrochen; die App wurde nicht zwangsweise beendet oder ersetzt. Der bekannte EÜR-Fehler bei ausländischer Umsatzsteuer bleibt separat offen; die Importqualität prüft der Eigentümer nach Installation.

**Release-Autorisierung präzisiert (2026-09-16).** Nach ausdrücklicher Freigabe des jeweiligen Releases dürfen Agenten künftig das bestehende `scripts/release.sh --notarize` mit eingerichteten Keychain-Zugängen ausführen, ohne Zugangsdaten auszulesen oder Zugriffsrechte zu ändern. Schlüsselanlage und Zugangseinrichtung bleiben Eigentümerschritte. Push, Veröffentlichung und Installation benötigen weiterhin ausdrückliche Eigentümerfreigabe; für 0.4.0 liegt sie vor.

**Minimaler Agentenkontext (2026-09-16).** Der dauerhafte Systemtext besteht aus drei kurzen Absätzen über Pfennig, Buchungsdaten und die Werkzeugarbeit sowie zwei Regeln: Ausgaben gelten beim Import als bezahlt, sofern das Dokument nichts Gegenteiliges erkennen lässt; ohne Zahlungsdatum gilt das Belegdatum. Nach dem 0.4.0-Reimport ergänzt: vollständig betriebliche Nutzung annehmen, sofern Dokument oder Nutzer keinen privaten Anteil angeben. Diese zweite Regel ist noch nicht veröffentlicht; bestehende Buchungen bleiben unverändert. Danach folgen neutrales Profil, die tatsächliche CREATE-Anweisung ausschließlich für `buchungen`, der Hinweis auf die von der Anwendung verwalteten Felder und nur die nach Einnahmen und Ausgaben gruppierten Kategorieschlüssel. Steuernummer, Kategoriebeschreibungen und bekannte Gegenparteien werden nicht geladen. Die Nutzer-Nachricht beschreibt mit Dateiname und Anhang, dass eine Datei hinzugefügt wurde. Der SQL-Autorisierer erlaubt SELECT, INSERT und UPDATE ausschließlich auf `buchungen`; andere Tabellen sind auch lesend gesperrt. Keine Schema-, UI- oder Exportänderung.

**Belegnummer und Fälligkeit (2026-09-16).** Die gemeinsame Tabelle `buchungen` trägt optional `belegnummer` und `faelligkeit`; beide stehen im Inspector. Überfällig wird aus Fälligkeit vor heute und nicht vollständig bezahlter Buchung abgeleitet. Der Agent übernimmt Dokumentangaben oder berechnet eine ausdrücklich genannte Zahlungsfrist. Ein Schema-CHECK verhindert, dass GRDB ein ungültiges optionales Datum still als nil liest. Keine Migration, Kompatibilitätsschicht, neue Ansicht oder zusätzliche Abstraktion. Verifikation: 110 Tests, SwiftFormat und `scripts/build.sh` erfolgreich; unabhängiges Review `08d3bd2e` freigegeben nach Entfernung eines unnötigen Kompatibilitätstests.

**Entwicklungsarchiv neu aufgebaut (2026-09-16).** Nach Eigentümerfreigabe und Beenden der App wurde die Datenbank aus der einzigen Schemadefinition neu erstellt. Einstellungen und exportierte Zeiträume sind unverändert erhalten; Buchungen, Dateien, Aktivitäten und Anfragen sind leer. Integrität und Belegkopien sind geprüft. Alte Datenbank und Archivoriginale liegen im privaten Backup `~/Library/Application Support/Pfennig/Backups/pfennig-schema-fields-20260916-102552/`, Reimportkopien unter `Zum erneuten Importieren/`. Kein automatischer Reimport. `/Applications/Pfennig.app` bleibt unverändert auf dem offiziellen Release; zum Testen des neuen Schemas dient `build/Build/Products/Debug/Pfennig.app`. Vor 1.0 bleiben Migrationen und Abwärtskompatibilitätscode auf ausdrückliche Eigentümerentscheidung ausgeschlossen.

**Live ledger integration (2026-09-15).** The approved refined native treatment is the actual Buchungen view. The window has a sidebar with the single item „Buchungen“; the intermediate preview pages, sample datasets and the HTML mockup were removed. The live table keeps its repository data flow and existing actions, adds the derived Geprüft / Zu prüfen / Beleg fehlt status column and the toolbar filters for direction and review status, uses the fixed 26-category symbol map, and keeps payment status separate. Review status only treats empty attachments as missing for Rechnung, Beleg and Gutschrift. A later window layout polish (`8c294fe`) was reverted in `c72d058`, so this state is what HEAD builds. Verification: 105 tests passed; 24 unique macOS SF Symbols validate all 26 category mappings; the independent review `1ee5982a` and the parent Core/UI/Workspace/docs review are approved; `scripts/build.sh` succeeds and the owner UI test is pending.

**Naming refactor (2026-09-15).** Infrastructure, agent mechanics, UI plumbing and generic helpers use English names, including the `Core` module and `LocalDate`. German accounting and persistence types, properties, raw values, schema names and JSON keys remain unchanged; naming-only changes preserve rendered prompts, errors and UI strings. Verification: 102 tests pass, the app build and release-contract checks pass, and SwiftFormat is clean. Independent source review f5e49310 approved the naming refactor.

**2026-09-14.** Der Neuaufbau nach `docs/specs/pfennig-neu.md` beginnt. Der alte Code ist als Tag `legacy-2026-09-14` archiviert und aus dem Arbeitsbaum entfernt. Erhalten: Spec, AGENTS.md, Skripte, `project.yml`, Info.plist, Entitlements, Assets, die Recherche zum UStVA-XML (Upload am 2026-09-14 erfolgreich getestet) und die Notizen zur Responses API.

**Schritt 1: Schema und Repository.** Das Paket hat ein Ziel `Kern` mit Tests. Es enthält die Werttypen `Cent` und `Datum`, die eine Schemadefinition mit den fünf Tabellen aus Abschnitt 2 der Spec (Aufzählungen als CHECK-Bedingungen, JSON-Struktur als Kommentar neben der Spalte, damit der Agent sie im Schematext liest) und die Datensätze `Buchung` mit `Position` und `Zahlung`, `Datei`, `Aktivitaet` und `Anfrage`. Das `Repository` öffnet die Datenbank aus einer Datei oder im Speicher, schreibt zu jedem Schreibvorgang an einer Buchung eine Zeile in `aktivitaeten` mit vorher und nachher, vergibt die Zahlungs-ids und bedient Einstellungen, Dateien und Anfragen; `Archivpfad` legt `~/Library/Application Support/Pfennig/` mit `Archiv/` und `Inbox/` an.

**Schritt 2: Oberfläche.** Das Ziel `App` gibt es, gebaut über `project.yml` mit XcodeGen. Ein Fenster mit einer `Table` der Buchungen, einer Fußzeile mit Einnahmen, Ausgaben und Saldo der sichtbaren Zeilen und dem `.inspector` rechts. In der Toolbar stehen das Dropdown Alle/Einnahmen/Ausgaben, das Suchfeld, ein Plus für eine neue Ausgabe von heute und der Weg in die Einstellungen. Der Inspector ist ein `Form` im Stil `.grouped` mit den flachen Abschnitten Grunddaten, Beträge, Steuer, Zahlungen und Notizen; er bearbeitet einen Entwurf und schreibt ihn beim Bestätigen eines Feldes über `Repository.speichern` zurück, ohne Sichern- oder Verwerfen-Knopf. Ungeprüfte Buchungen tragen einen Punkt in der Zeile und einen Bestätigen-Knopf unten im Inspector. Die Einstellungen sind das normale Fenster mit Profil und Erscheinungsbild.

Neu im `Kern`: die feste Kategorienliste, das Profil in den Einstellungen, `Uebersicht` mit Filter, Suche und Summen, `Formate` mit dem Lesen und Schreiben von Beträgen und Daten für die Textfelder, `Position.steuer` und `Repository.loeschen`.

**Schritt 3: der Agent mit dem sql-Werkzeug.** Das Paket hat ein zweites Ziel `Agent` mit Tests. Im `Kern` liegen die neuen Bausteine, die an der Datenbank hängen: `Autorisierer` und `Werkzeug` mit dem sql-Aufruf und `Pruefregeln` mit den acht Inhaltsregeln aus Abschnitt 4 der Spec. Der `Agent` enthält `Schluesselbund` (Schlüsselbund, unveränderte Kennungen aus der alten App), `Responses` (ein Aufruf von `POST /v1/responses`, eine Wiederholung bei 429 und 5xx), `Anleitung` (kurzer Systemtext, Profil, tatsächliches `buchungen`-Schema und gruppierte Kategorieschlüssel), `Agentenlauf` (die Werkzeugschleife, Modell `gpt-5.6-luna`, Denkaufwand medium, höchstens 25 Werkzeugaufrufe, Aufzeichnung in `anfragen`) und `Eingang` (Hash, Inbox, Lauf, Archiv, Dedupe über den Hash, Aufräumen der Buchungen eines abgebrochenen Laufs).

Der Autorisierer sitzt nicht auf der Verbindung der App: GRDB setzt dort seinen eigenen und braucht ihn für die Änderungsbeobachtung. Stattdessen compiliert das Werkzeug die Anweisung des Agenten auf einer zweiten, leeren Verbindung mit demselben Schema, auf der der Autorisierer liegt. Die Entscheidung hängt nur an Anweisung und Schema, also ist eine Anweisung, die dort compiliert, genau eine erlaubte Anweisung. Ausgeführt wird sie danach auf der echten Verbindung in einer Transaktion; berührte Zeilen ergeben sich aus dem Vergleich vor und nach dem Aufruf, Swift setzt darauf Zeitstempel, `belege`, `geprueft_am` und die Zahlungs-ids, schreibt je eine Zeile in `aktivitaeten` und committet nur, wenn alle Prüfregeln halten. Der aktuelle Autorisierer und der daraus abgeleitete angezeigte Schematext umfassen ausschließlich `buchungen`.

Neu in der Oberfläche: Drag-and-drop auf das ganze Fenster (pdf, png, jpg, jpeg, heic, csv), ein Fortschrittsanzeiger „n von m“ in der Toolbar, solange die Warteschlange läuft, eine schmale Leiste über der Tabelle für gescheiterte Dateien mit „Erneut versuchen“ und „Verwerfen“, der Beleg-Abschnitt im Inspector mit Dateinamen, Vorschau und „Original öffnen“, und der Reiter KI-Zugang in den Einstellungen. Dateien werden nacheinander abgearbeitet; beim Start nimmt sich die App, was in der Inbox liegt.

Nach dem Test des Nutzers nachgezogen: Das Erscheinungsbild sitzt jetzt auf `NSApp.appearance`, weil `preferredColorScheme` die Toolbar nicht erreicht. Der Eingang arbeitet in einer Warteschlange mit genau einer Aufgabe, der Anzeiger zählt die laufende Datei. Ein Hash gilt nur dann als bekannt, wenn eine Buchung ihn noch in `belege` trägt; sonst geht die Datei erneut an den Agenten, und „bereits vorhanden“ steht sichtbar in der Leiste über der Tabelle. Beim Löschen einer Buchung verschwinden Zeile in `dateien` und Original im Archiv, sobald keine Buchung den Beleg mehr trägt. Dazu: „Verbindung testen“ im KI-Zugang, ein Rahmen, während eine Datei über dem Fenster hängt, Symbolknöpfe im Beleg-Abschnitt (Vorschau, Original öffnen, vom Beleg nehmen), HEIC fällt weg, `Archivpfad` ist ein Wert mit `standard` und macht den Eingang testbar.

Nach der zweiten Runde Tests entschieden: Dateien laufen nebeneinander, höchstens zehn gleichzeitig, und der Anzeiger zählt die fertigen; jede Anweisung geht weiterhin durch die eine Datenbankschlange, und zwei gleiche Dateien in einem Wurf teilen sich einen Lauf. Der KI-Zugang trägt jetzt Modell (Sol, Terra, Luna; Luna ist die Vorgabe), Denkaufwand und den Schalter „Schnellere Verarbeitung“, gespeichert unter `ki.modell`, `ki.aufwand` und `ki.schnell`; der Schalter setzt `service_tier: "priority"`, OpenAIs Fast mode zum etwa doppelten Preis (Abschnitt 8 der API-Notizen). Die Spalte Zahlung heißt Bezahlt, und ein Klick auf das Symbol bezahlt den offenen Rest mit heutigem Datum oder nimmt alle Zahlungen wieder weg.

Nächster Schritt: der Export nach Abschnitt 5, UStVA als XML und EÜR als CSV.

**Abend 2026-09-14.** Abschnitte 1 bis 3 sind auf main: Kern (Schema, Repository, Prüfregeln, sql-Werkzeug mit Autorisierer), Agent (Responses-Schleife, Anleitung, Eingang, Keychain) und App (Tabelle, Inspector, Einstellungen, Drag-and-drop). Vier echte Rechnungen wurden erfolgreich verarbeitet. Produktion rund 3.700 Zeilen, 50 Tests.

**Schritt 4: der Export.** Im `Kern` liegen `Zeitraum` (Monat, Quartal oder Jahr, mit Frist nach §18 Abs. 1 UStG und der Vorauswahl aus Rhythmus und Dauerfristverlängerung), `Aufteilung` (die kumulative Verteilung einer Zahlung auf die Positionen nach größtem Rest), `Kennzahl` (die gegen das BMF-Vordruckmuster 2026 geprüfte Liste), `UStVA` (Ist-Versteuerung: Einnahmen je Zahlung, Vorsteuer bei max(Beleg, Zahlung), §13b zum Belegdatum, Kleinunternehmer nur §13b, Zahllast wie ELSTER aus vollen Euro), `EUeR` (Jahreswerte je Formularzeile nach Zufluss und Abfluss, ohne Privatanteil, brutto beim Kleinunternehmer, dazu das CSV) und `UStVAXml` (der verifizierte Aufbau für den Upload). `Kategorie` trägt jetzt die EÜR-Zeile, das Schema die sechste Tabelle `zeitraeume` (jahr, art, idx, exportiert_am) und das Repository `exportVermerken` und `exportierteZeitraeume`.

In der Oberfläche steht „Export“ in der Toolbar und öffnet ein Sheet mit Formular, Zeitraumwahl, den Werten, einem Hinweis auf ungeprüfte Buchungen im Zeitraum, einem Hinweis auf einen bereits exportierten Zeitraum, dem Link zu Mein ELSTER und dem Sichern über das Standardfenster. Der Inspector warnt „Zeitraum bereits exportiert“, wenn ein Datum der Buchung in einem exportierten Zeitraum liegt.

Nachlese des Reviews: Namensraum und `version` der XML folgen dem Jahr des Zeitraums (`v2026`/`version="2026"`), wie die am 14.09.2026 angenommene Datei zeigt; die EÜR-Vorauswahl folgt der Frist 31. Juli statt einer festen Jahresmitte; der Hinweis im Inspector erscheint nur, wenn die Buchung nach dem Export geändert wurde.

Offen und bewusst nicht gebaut: das Datenmodell kennt keine innergemeinschaftliche Lieferung, Ausfuhr, innergemeinschaftlichen Erwerb und keine Einfuhrumsatzsteuer, also bleiben die geprüften Kennzahlen 41, 43, 89, 93, 61 und 62 leer; §13b kennt nur das Belegdatum, keinen Leistungszeitraum; Ausnahmenlisten und die Einzelaufstellung je Kennzahl aus der alten App fallen weg. Die EÜR-Zeilennummern stammen aus dem Formular 2023/2024 und sind für 2026 ungeprüft. Vom Eigentümer entschieden ist die Umsatzsteuer in der EÜR: sie folgt jetzt durchgehend der Methode der amtlichen Anlage EÜR. Die Kategoriezeilen bleiben netto, dazu kommen bei Regelbesteuerung zwei berechnete Zeilen, die vereinnahmte Umsatzsteuer aller Einnahmezahlungen des Jahres als Einnahme (Zeile 18) und die gezahlte Vorsteuer aller Ausgabezahlungen des Jahres als Ausgabe (Zeile 59, Privatanteil wie beim Nettobetrag abgezogen, Erstattungen negativ). Mit `ust_zahlung` (Zeile 60) und `ust_erstattung` (Zeile 16) sind das die vier Umsatzsteuerzeilen des Formulars, und der Gewinn stimmt wieder. Reverse-Charge-Ausgaben tragen Steuer 0 und liefern hier keine Vorsteuer; die §13b-Vorsteuer ist eine Sache der UStVA. Beim Kleinunternehmer ändert sich nichts: brutto je Zeile, keine Umsatzsteuerzeilen, `ust_zahlung` weiter für die gezahlte §13b-Steuer. Die Nummern 18 und 59 sind wie die übrigen Zeilennummern ungeprüfte Platzhalter aus dem Formular 2023/2024.

**Entwicklungsdatenbank neu aufgebaut (2026-09-15).** Auf Eigentümerfreigabe wurde `pfennig.sqlite` aus der aktuellen einzigen Definition in `Sources/Core/Schema.swift` neu erstellt. Die bisherigen Datenbankhistorien und die 11 Archivoriginale liegen im privaten, zeitgestempelten Backup unter `~/Library/Application Support/Pfennig/Backups/pfennig-development-rebuild-20260915-141953/`; die Einstellungen wurden erhalten. Buchungen, Dateien, Aktivitäten, Anfragen und Exportzeiträume sind für den kontrollierten Neueinlauf geleert. Die vorbereiteten Reimportkopien liegen dort in `Zum erneuten Importieren`; der Eigentümerimport steht aus. Kein automatischer Reimport. Die unabhängige Prüfung `17385272` bestätigte Integrität, Schema, Einstellungserhalt und Dateihashes ohne Beanstandung.

**Release 0.1.0 (2026-09-15).** Tag `v0.1.0`, Developer-ID-signiert, von Apple notarisiert und gestapelt, `dist/Pfennig-0.1.0-macOS.zip` mit SHA-256. GitHub-Release als Entwurf angelegt, Veröffentlichung ist der Schritt des Eigentümers. Siehe `CHANGELOG.md`.

**Sicherheitsfix 2026-09-15.** „Verwerfen“ löscht ausschließlich Dateien, deren aufgelöster übergeordneter Pfad die Inbox ist. Fehler vor dem Kopieren behalten den Originalpfad und lassen das Original unberührt; Tests decken Inbox-Kopie, Geschwisterpfad und ein blockiertes Inbox-Verzeichnis ab. Die unabhängige Prüfung `67787b65` bestätigte den Fix ohne Beanstandungen.

**Import-Finalisierung.** Eine abgeschlossene Agentenantwort ohne angelegte oder geänderte Buchung ist ein Fehler und lässt die Inbox-Datei für den bestehenden Wiederholungs- und Verwerfen-Ablauf liegen. Das Dokument wird zuerst ins Archiv kopiert; Dateizeile und Beleganhänge werden in einer Transaktion gespeichert. Erst nach deren Commit wird die Inbox-Kopie entfernt. Schlägt die Datenbank-Finalisierung fehl, bleibt die Inbox erhalten; eine übrig gebliebene Archivkopie wird beim Wiederholen verwendet.

**Review 94713037.** Die Import-Finalisierung wurde unabhängig geprüft und ohne Beanstandungen freigegeben.

**Inspector-Stale-Draft-Fix.** Bei einer Datenbankänderung der ausgewählten Buchung ersetzt der Inspector jetzt Entwurf und gespeicherten Ausgangszustand durch die neueste vollständige Zeile, auch bei ungespeicherter Eingabe. Das akzeptiert seltenen Verlust laufender Eingabe, verhindert aber, dass ein veralteter kompletter Entwurf neuere Zahlungen oder Belege überschreibt. Es gibt bewusst keine Feldzusammenführung oder Versionssperre. Die unabhängige Prüfung `820f48dd` gab den Fix ohne Beanstandungen frei.

**Reviewstatus-Agentenänderung.** Jede tatsächliche Agentenänderung setzt `geprueft_am` auf leer; auch das Anhängen eines Belegs zählt. Nutzerbestätigung und Nutzeränderungen erhalten den Prüfstatus, reine SELECT- und No-op-Anweisungen ändern ihn nicht. Die unabhängige Prüfung `6240ca66` gab den Fix ohne Beanstandungen frei.

**Kategorie-Richtungsprüfung.** Bekannte Kategorien müssen jetzt zur Richtung der Buchung passen; bei Abweichung antwortet die bestehende Prüfregel mit „Kategorie passt nicht zur Richtung.“ und rollt den Schreibvorgang zurück.

**Fremdwährungswerkzeug 2026-09-15.** Fremdwährungsbelege behalten `waehrung` und `originalbetrag` als exakte Dezimalzahl in Haupteinheiten; die EUR-Positionen bleiben Cent. Der Agent hat neben `sql` genau ein Werkzeug `umrechnen(waehrung, datum, betraege)`. Es ruft einmal den historischen Frankfurter-v2-Referenzkurs des Standard-Blends ab, überträgt nur Währung und Datum an die API und rechnet alle Beträge lokal mit Swift `Decimal` in EUR-Cent um. Ergebnis, Kursdatum und Quelle stehen in der bestehenden Anfragekonversation. Frankfurter ist dabei keine bank- oder steuerlich vorgeschriebene Kursquelle. Synthetic transports cover request privacy, Decimal rounding for USD/ZAR/JPY/KWD-like values, returned versus requested dates, errors, mixed tool dispatch and logs.

**Konsolidiertes Review 2026-09-15.** `Luna e4ad6720` fand den fehlenden numerischen Kurs in der Buchungsnotiz; `Gemini 764c0374` fand das Blockieren anderer Inspector-Änderungen bei ungültigem Originalbetrag; `GLM f0bb0f25` bestätigte den insgesamt schlanken Stand und zwei objektive Bereinigungen. Alle drei bestätigten Punkte sind umgesetzt. Spekulative Vereinfachungen und Stiländerungen wurden nicht übernommen. Die unabhängige Schlussprüfung durch `e4ad6720` gab die Korrekturen nach 102 Tests, Formatierung und Diffprüfung frei.

**Sparkle-Aktualisierungen (2026-09-15).** Sparkle 2.10.0 ist als SPM-Abhängigkeit nur am App-Ziel eingebunden. Die Standardoberfläche startet mit normalem Hintergrundprüfzyklus und Installationsbestätigung; „Nach Updates suchen“ steht im App-Menü und folgt `canCheckForUpdates`. `SUFeedURL` zeigt auf das stabile GitHub-Release-Asset, `SUPublicEDKey` enthält den vom Eigentümer erzeugten öffentlichen Schlüssel. Die Update-Archivdatei enthält ausschließlich die mit Developer ID signierte und von Apple notarisierte App; `scripts/release.sh --notarize` erzeugt daraus mit Sparkles offiziellem `generate_appcast` eine signierte `appcast.xml` samt Prüfsummen und ohne Deltas. Die Feed-URL bleibt bis zum Anhängen und Veröffentlichen von `appcast.xml` in einem GitHub-Release unzugänglich. Die Vorbereitung 0.2.0/build 2 und die Integration wurden durch `dc944e1b` freigegeben. Der Eigentümer muss die Ausgabe mit zwei signierten Releases prüfen; kein Update- oder Veröffentlichungsaufruf wurde von diesem Schritt ausgeführt.

**Release-Pipeline-Fix (2026-09-15).** Nach Apples Ablehnung von 0.2.0/build 2 exportiert `scripts/release.sh` das Archiv jetzt mit `xcodebuild -exportArchive` und `method = developer-id`, statt das rohe Archiv zu verpacken. Vor dem Upload prüft es die vier Sparkle-Helfer auf Developer-ID-Team und sicheren Zeitstempel. Distribution ZIP, Sparkle-Update-ZIP und Appcast verwenden dieselbe exportierte App. Bei einem ungültigen Notarisierungsergebnis lädt das Skript Apples Diagnose automatisch nach `dist/notary-log.json`; der Eigentümer muss den signierenden Lauf erneut ausführen.

**Review-Nachbesserungen (2026-09-15).** `generate_appcast` verwendet nach Prüfung des installierten Sparkle-2.10.0-Tools die Option `-o`; `--output-path` wird nicht unterstützt. `Timestamp=Not Set` gilt ausdrücklich nicht als sicherer Zeitstempel. Die exportierte Signatur verwendet außerdem dieselbe vollständige `SIGNING_IDENTITY`, die der Archivschritt bereits geprüft hat. `scripts/release-contract-test.sh` deckt diese Verträge ohne Signieren oder Schlüsselbundzugriff ab.

**Release und Installation 0.2.0 (2026-09-15).** Das öffentliche GitHub-Release `v0.2.0` ist als Latest mit genau sechs Assets veröffentlicht. Unauthentifizierte HTTPS-Downloads der Assets und des Latest-Appcasts stimmen byteweise mit den geprüften Dateien überein und bestehen die Prüfsummen; die signierte Appcast-Prüfung mit dem öffentlichen Schlüssel ist durch `cfe31bf1` belegt. `/Applications/Pfennig.app` ist auf Version 0.2.0, Build 2 aktualisiert; die vorherige App ist separat gesichert. Der einmalige History-Scan `agent4c142ba2` fand keine bestätigten Geheimnisse oder privaten Bankdaten; vorhandene Dokument-Fixtures wurden als synthetisch bestätigt. Der interaktive Sparkle-Aktualisierungszyklus ist weiterhin nicht getestet und wird vom Eigentümer mit einem folgenden signierten Release geprüft.

**Englische Namen im Agent-Modul (2026-09-15).** Das Modul `Agent` und die Infrastruktur in `Core` (`SQLTool`, `SQLAuthorizer`, `Schema`, `ArchivePaths`, `Repository`) verwenden jetzt durchgehend englische Bezeichner. Deutsch bleiben: persistierte Modelle und ihre Eigenschaften (`Buchung`, `Datei`, `Aktivitaet`, `Anfrage`, `Profil`), alle Schemanamen, Speicher- und Werkzeug-JSON-Schlüssel (`waehrung`, `datum`, `betraege`, `werkzeug`, `argumente`, `ergebnis`, `fehler`), der Status `.fehler` in `anfragen` sowie jeder gerenderte Text. Ein Literalvergleich über den Diff belegt, dass sich nur interpolierte Bezeichner geändert haben, kein Prompt-, Fehler- oder UI-Text. Verifikation: 105 Tests grün, App-Build grün, SwiftFormat sauber.

**Einstellungen in der Seitenleiste (2026-09-15).** Der Zahnrad-`SettingsLink` steht nicht mehr in der Werkzeugleiste, sondern als „Einstellungen“ am Fuß der Seitenleiste.

**Englische Namen abgeschlossen (2026-09-15).** Der Umbenennungsdurchgang ist beendet. Zuletzt wurden `KiEinstellungen`, `Modell` und `Denkaufwand` zu `AISettings`, `Model` und `ReasoningEffort` mit englischen Fallnamen; die Speicherschluessel `ki.modell`, `ki.aufwand`, `ki.schnell` und ihre Werte bleiben unveraendert, ebenso `Anfrage.modell` als Spalte. Deutsch bleiben nur noch Buchhaltungs- und persistierte Modelle, Schemanamen, Werkzeug-JSON-Schluessel, gerenderte Texte und die deutschen Testnamen in `Tests/`; die Testnamen bleiben auf Entscheidung des Eigentuemers so.

**Release 0.3.0 (2026-09-15).** Marketingversion 0.3.0, Build 3. Der Inhalt gegenueber 0.2.0 ist die ueberarbeitete Buchungsansicht mit fester Seitenleiste, festen Spaltenbreiten, festem Inspektor neben der Tabelle statt darueber, Kategoriesymbolen aus `Kategorie`, Einstellungen am Fuss der Seitenleiste, Logo-Wasserzeichen im leeren Inspektor sowie der abgeschlossene englische Umbenennungsdurchgang. Das Datenbankschema ist gegenueber 0.2.0 byteweise unveraendert; dieses Release ist der erste echte Test des Sparkle-Aktualisierungszyklus von 0.2.0 auf 0.3.0.
